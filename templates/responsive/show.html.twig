{% extends 'base.html.twig' %}

{% block title %}Responsive{% endblock %}

{% block body %}
    <h1>Responsive</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsive.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ responsive.image }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ responsive.updatedAt ? responsive.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('responsive_index') }}">back to list</a>

    <a href="{{ path('responsive_edit', {'id': responsive.id}) }}">edit</a>

    {{ include('responsive/_delete_form.html.twig') }}
{% endblock %}
